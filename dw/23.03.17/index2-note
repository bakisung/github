Q. 버튼을 클릭했을때 내가 입력한 값과 
콤피타에서 선정한 값이 같은 경우 정답처리
다른 경우 오답처리 단, 숫자는 1~10까지만 허용된다.

1.HTML 화면을 구성한다 p태그, 인풋, 클릭버튼과 결과값을 출력해줄 div를 생성한다.
2.CSS는 건너뛴다.
3.script에 연결할 js파일을 생성한다.
4.대상들을 선언한다. 대상으로는 input태그 button태그 div태그이다.
5.함수값을 적는다
6.함수값에는 버튼을 클릭했을때에 내가 입력한 값과 콤피타가 출력한 값을 비교한다.
7.숫자의 조건은 1~10 까지이고 동일하면 참, 다르다면 거짓으로 호출한다.
8.숫자 조건을 벗어나면 결과값을 출력해주는것이 아니라 경고문을 출력해준다.



Q. 펑션밖에 const or let 선언과 안에서 선언할때의 차이점
펑션 함수 입력 (버튼이 클릭 되었을때)
input 태그 선언
input에 입력되는 텍스트를 숫자 10진법으로 변경
랜덤숫자 선언 (소숫점 내림* 맥스-미니멈 = ? + 1)
결과값을 출력할 div 선언



정답에 대한 조건을 걸고, 버튼을 클릭했을때 입력한 숫자에 따라 정답인지 아닌지 결과값을 호출
1. 문자로 입력할 경우 숫자로 입력하라는 문구가 나와야함





2. 1~10 사이의 숫자만 입력해야한다는 문구






3. 입력한 숫자와 랜덤으로 출력된 숫자가 동일하다면 정답 다르다면 오답 문구








