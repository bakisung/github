package h_api;

import java.text.DecimalFormat;
import java.text.Format;
import java.util.Scanner;

public class SetComma {
		
	
	
//	void comma() {
//		if () {
//			
//		}
//	}
	
	public static void main(String[] args) {
		
//=================================================================================================
		Scanner s = new Scanner(System.in);
		System.out.println("숫자를 입력하시오.");
		String inputNum = s.nextLine();
		int count = 0;
		String rev = "";

		for (int i = inputNum.length() - 1; i >= 0; i--) {
			rev += inputNum.charAt(i);
			count++;
			if (count == 3) {
				rev += ",";
			}
		}
		
		for (int i = inputNum.length() - 1; i >= 0; i--) {
			System.out.print(rev.charAt(i));
		}
//		System.out.println(inputNum);
//=================================================================================================
		// 숫자를 입력받아서 3자리마다 콤마(,)를 붙여 출력해주세요.
		// 1000000 ==> 1,000,000
//		Scanner s = new Scanner(System.in);
//		for ( ; ; ) {
//		System.out.println("숫자를 입력하시오.");
//		int inputNum = Integer.parseInt(s.nextLine());
//
//		DecimalFormat df = new DecimalFormat("#,###");
//		String inputNumber = df.format(inputNum);
//		System.out.println("변환값은 " + inputNumber + " 입니다.");
//		}
//=================================================================================================
//		int number = 123456789; // 입력한 숫자
//        StringBuilder sb = new StringBuilder(Integer.toString(number)); // StringBuilder 객체 생성
//        int length = sb.length(); // 문자열 길이 계산
//        for (int i = length - 3; i > 0; i -= 3) { // 3자리마다 쉼표 추가
//            sb.insert(i, ",");
//        }
//        String formattedNumber = sb.toString(); // 쉼표가 포함된 문자열 생성
//        System.out.println(formattedNumber); // 결과 출력
		
//=================================================================================================
	}

}
